function main(list) {
    list.map(function(element) {
        return console.log(element);
    });
}

main(['Hello','my','friends.']);
main(['This','is','another','list.']);
main(['Function','is being','excecuted','for every','element','of the list.']);